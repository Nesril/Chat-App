import React from 'react'
import Header from '../header_footer/header'
export default function mygroup({setDarkmode,darkmode}) {
  return (
    <div>
       <Header darkmode={darkmode} setDarkmode={setDarkmode}/>
      mygroup</div>
  )
}
